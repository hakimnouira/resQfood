<form method="post" action="{{ path('app_donation_delete', {'donationId': donation.donationId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ donation.donationId) }}">
    <button class="btn">Delete</button>
</form>
